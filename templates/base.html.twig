<!DOCTYPE html>
<html>
	{# Head html #}
	{% include "partials/_headHtml.html.twig" %}

	<body class="flex flex-col justify-between  " {{ stimulus_controller('symfony/ux-swup/swup', { animateHistoryBrowsing: true, animationSelector: '[class*="transition-"]', plugins: '[direction: "to-right"]',cache: true, theme: 'overlay', debug: true, } ) }}>

		{% set currentPage = app.request.get('_route') %}

		{% include "partials/_header.html.twig"%}

		<main class=" w-full h-auto font-gluten font-medium bg-gradient-to-br from-stone-100 from-10% via-stone-400 via-40% to-blue-950 to-90%  "> {% block body %}{% endblock %}
			</main>

			{% include "partials/_paymentByCard.html.twig" %}
			{% include "partials/_footer.html.twig" %}

		</body>
	</html>
